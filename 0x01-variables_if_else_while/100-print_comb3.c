#include <stdio.h>

/**
 * main - prints different combinations of two digits
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int x, y;

	for (x = 48; x < 58; x++)
	{
		putchar(x);
	}
	for (y = 48; y < 58; y++)
	{
		putchar(y);
	}
	if (x == y || y <- x)
	{
		putchar(34);
		putchar(' ');
	}
	else if (x == 56 && y == 57)
	{
		
	}
	putchar('\n');
	return (0);
}
